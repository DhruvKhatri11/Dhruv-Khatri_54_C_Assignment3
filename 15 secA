#include <stdio.h>
int main() {
    int a[10], n, i;
    printf("Enter size: ");
    scanf("%d",&n);
    for(i=0;i<n;i++) scanf("%d",&a[i]);
    for(i=0;i<n-1;i++) a[i]=a[i+1];
    n--;
    for(i=0;i<n;i++) printf("%d ",a[i]);
    return 0;
}

Output: Enter size: 1 2 3 4 5
2 3 4 5
